cmake_minimum_required (VERSION 2.6)
project (sha256sum_test)
add_executable(sha256sum_test main.c test.c tests.h)
add_dependencies(sha256sum_test sha256sum)
target_link_libraries(sha256sum_test sha256sum) 
